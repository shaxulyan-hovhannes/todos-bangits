import { FC } from "react";

import Layout from "components/layout";
import MiniDrawer from "components/drawer";

const App: FC = () => {
  return (
    <Layout>
      <MiniDrawer />
    </Layout>
  );
};

export default App;
